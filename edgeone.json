{
  "name": "moontv",
  "build": {
    "command": "pnpm install && pnpm gen:runtime && pnpm gen:manifest && pnpm next build && pnpm dlx @cloudflare/next-on-pages",
    "output": ".vercel/output/static"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ],
  "env": {
    "SITE_NAME": "MoonTV"
  }
}