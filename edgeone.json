{
  "name": "moontv",
  "build": {
    "command": "npm install --force && npm run build && npx @cloudflare/next-on-pages",
    "output": ".vercel/output/static"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ],
  "env": {
    "SITE_NAME": "MoonTV"
  }
}