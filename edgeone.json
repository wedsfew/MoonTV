{
  "name": "moontv",
  "build": {
    "command": "npm install --legacy-peer-deps && npm run pages:build",
    "output": ".vercel/output/static"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ],
  "env": {
    "SITE_NAME": "MoonTV"
  }
}